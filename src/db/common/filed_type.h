namespace wa{
    namespace db{
        /**
         * @brief 表示一个字段的类型
         */
        enum FieldType{
            INT,
            CHARS,
            FLOAT,
            DOUBLE,
            VECTOR,
            CHAR,
            VARCHAR,
            DATE,
            BOOLEAN
        };
    }
}